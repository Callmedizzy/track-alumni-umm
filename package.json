{
  "name": "alumni-tracker-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "cd backend && .\\venv\\Scripts\\activate && python -m uvicorn app.main:app --reload",
    "build": "npm run build --prefix frontend",
    "start": "run_dev.bat"
  }
}
